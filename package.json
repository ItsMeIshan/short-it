{
  "name": "url-shortner",
  "version": "1.0.0",
  "description": "A URL shortening app that redirects to the URL that user provides and makes convenient to share the short URLs.",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ishan Soni",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "http-status-codes": "^2.1.4",
    "mongoose": "^6.0.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
